import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'home_budget_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
